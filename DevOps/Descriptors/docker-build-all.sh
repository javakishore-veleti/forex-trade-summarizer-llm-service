THIS_SCRIPT_DIR="$(dirname $0)"
LOG_PREFIX="docker-build-all.sh"

# Disabled building because for this project docker-run-simple makes it easy to setup Apache Nifi
# echo "${LOG_PREFIX} THIS_SCRIPT_DIR=${THIS_SCRIPT_DIR}"
# echo "${LOG_PREFIX} STARTED Building Docker Images"

# echo "${LOG_PREFIX} STARTED Building Apache-Nifi Docker Image"
# . "${THIS_SCRIPT_DIR}"/Apache-Nifi/docker-build.sh

# echo "${LOG_PREFIX} COMPLETED Building Docker Images"
